version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user/schoolyard
hooks:
  AfterInstall:
    - location: deploy-scripts/install-and-run.sh
      timeout: 600
      runas: ec2-user